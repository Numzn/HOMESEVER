# Networks (optional)

The **infra** bridge network is created by the [postgres stack](../postgres/docker-compose.yml) (`name: infra`).

Add standalone network definitions here only if you need networks that are not tied to a specific stack.
